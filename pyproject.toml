[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trading-sdk"
version = "0.1.0"
description = "Strategy SDK for the trading engine"
requires-python = ">=3.11"
dependencies = []
